{% extends 'admin/layout.html.twig' %}

{% block body %}
    <h1>ConceptMediaType</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conceptMediaType.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ conceptMediaType.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_conceptmediatype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_conceptmediatype_edit', { 'id': conceptMediaType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'admin/layout.html.twig' %}

{% block body %}
    <h1>ProductMediaType</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productMediaType.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ productMediaType.label }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_productmediatype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_productmediatype_edit', { 'id': productMediaType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
